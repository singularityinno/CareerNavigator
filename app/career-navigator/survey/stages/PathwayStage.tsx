import { useSurveyContext } from "@/career-navigator/contexts";
import React from "react";

const PathwayStage = () => {
  const { setCurrStage } = useSurveyContext();
  return <div>Pathway Stage</div>;
};

export default PathwayStage;
